<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.hyuwah.sandbox.ane">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="dev.hyuwah.sandbox.ane.simple.retrofit.callback.SimpleRetrofitCallbackActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name="dev.hyuwah.sandbox.ane.simple.retrofit.rxjava.SimpleRetrofitRxJavaActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name="dev.hyuwah.sandbox.ane.simple.retrofit.coroutine.SimpleRetrofitCoroutineActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name="dev.hyuwah.sandbox.ane.simple.url.coroutine.SimpleUrlCoroutineActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name="dev.hyuwah.sandbox.ane.simple.url.asynctask.SimpleUrlAsyncTaskActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name="dev.hyuwah.sandbox.ane.simple.asynchttp.AsyncHttpActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".mvvm.retrofit.coroutine.presentation.RetrofitCoroutineActivity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>